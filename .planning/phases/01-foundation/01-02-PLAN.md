---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/renderer/shared/styles/globals.css
  - src/renderer/App.tsx
  - src/renderer/shell/Sidebar.tsx
  - src/renderer/shell/WindowChrome.tsx
  - src/renderer/shell/ModuleHeader.tsx
  - src/renderer/main.tsx
autonomous: true
requirements:
  - SHELL-03
  - SHELL-04
  - SHELL-05

must_haves:
  truths:
    - "The app renders with a dark, dense, handcrafted UI — blue-grey charcoal background, no white or generic gray"
    - "A narrow icon-only sidebar (52px) shows Dashboard, Habits, Planner, Expenses icons with a Settings gear at the bottom"
    - "Clicking a sidebar icon switches the active module view instantly (no loading state, no route transition)"
    - "The window has a custom frameless chrome with a drag region and window controls"
    - "A thin header bar shows the active module name"
    - "All visual values come from CSS custom properties — no hardcoded hex in component files"
  artifacts:
    - path: "src/renderer/shared/styles/globals.css"
      provides: "Complete design token system as CSS custom properties"
      contains: "@theme"
      min_lines: 40
    - path: "src/renderer/shell/Sidebar.tsx"
      provides: "Icon-only navigation sidebar"
      contains: "sidebar-width"
    - path: "src/renderer/shell/WindowChrome.tsx"
      provides: "Frameless window drag region and controls"
      contains: "webkit-app-region"
    - path: "src/renderer/shell/ModuleHeader.tsx"
      provides: "Thin header bar with module name"
      contains: "ModuleHeader"
    - path: "src/renderer/App.tsx"
      provides: "Root component with state-based module switching"
      contains: "activeModule"
  key_links:
    - from: "src/renderer/App.tsx"
      to: "src/renderer/shell/Sidebar.tsx"
      via: "import and render with activeModule + onNavigate props"
      pattern: "Sidebar.*activeModule"
    - from: "src/renderer/App.tsx"
      to: "src/renderer/shell/WindowChrome.tsx"
      via: "import and render at top of layout"
      pattern: "WindowChrome"
    - from: "src/renderer/shell/Sidebar.tsx"
      to: "lucide-react"
      via: "icon imports"
      pattern: "import.*lucide-react"
    - from: "src/renderer/shared/styles/globals.css"
      to: "src/renderer/main.tsx"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Build the complete design token system and shell layout — globals.css with all CSS custom properties, the icon-only sidebar, frameless window chrome, module header, and state-based navigation in App.tsx.

Purpose: This is the visual identity of Monolith. Every future component inherits its look from these tokens and renders within this shell. The sidebar, header, and window chrome are the permanent scaffolding that module content renders inside.
Output: A running app with the dark, dense, handcrafted aesthetic — sidebar navigation between placeholder module views, frameless window with drag region.
</objective>

<execution_context>
@C:/Users/binsl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/binsl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01 — types the executor needs -->

From src/shared/ipc-types.ts:
```typescript
export interface AppSettings {
  dateFormat: 'DD/MM/YYYY' | 'MM/DD/YYYY';
  notificationTime: string;
  windowBounds?: { width: number; height: number; x: number; y: number };
}

export interface SettingsAPI {
  get: () => Promise<AppSettings>;
  set: (settings: Partial<AppSettings>) => Promise<void>;
}

export interface API {
  settings: SettingsAPI;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the complete design token system in globals.css with Inter font setup</name>
  <files>
    src/renderer/shared/styles/globals.css,
    src/renderer/main.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Design System, Spacing Scale, Typography, Color, Motion Contract, Accessibility Contract, Implementation Notes sections),
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Design Token System),
    src/renderer/main.tsx (current state from Plan 01)
  </read_first>
  <action>
1. Create src/renderer/shared/styles/globals.css with the full design token system. Use Tailwind v4 @theme directive. The exact tokens from UI-SPEC.md:

```css
@import "tailwindcss";

@theme {
  /* Surface palette — cool charcoal dark theme */
  --color-bg-base: #16161e;
  --color-bg-elevated: #1a1a2e;
  --color-bg-overlay: #1e1e30;
  --color-bg-subtle: #22223a;

  /* Text hierarchy */
  --color-text-primary: #e8e8f0;
  --color-text-secondary: #9494a8;
  --color-text-muted: #5a5a72;
  --color-text-disabled: #3a3a52;

  /* Accent — electric indigo blue */
  --color-accent: #6366f1;
  --color-accent-hover: #818cf8;
  --color-accent-subtle: rgba(99, 102, 241, 0.15);

  /* Border */
  --color-border: rgba(255, 255, 255, 0.07);
  --color-border-focused: rgba(99, 102, 241, 0.5);

  /* Semantic */
  --color-destructive: #ef4444;

  /* Typography — 4 sizes only, 2 weights only (400, 600) */
  --font-size-small: 11px;
  --font-size-body: 13px;
  --font-size-heading: 15px;
  --font-size-display: 18px;

  --line-height-tight: 1.3;
  --line-height-normal: 1.5;

  /* Spacing — 4px grid, no 12px or 20px steps */
  --space-1: 4px;
  --space-2: 8px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;

  /* Layout */
  --sidebar-width: 52px;
  --drag-region-height: 36px;

  /* Radius */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;

  /* Transitions */
  --duration-fast: 80ms;
  --duration-normal: 150ms;
}
```

2. After the @theme block, add base styles:

```css
body {
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
  font-size: var(--font-size-body);
  line-height: var(--line-height-normal);
  background-color: var(--color-bg-base);
  color: var(--color-text-primary);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  user-select: none;
  margin: 0;
  padding: 0;
  overflow: hidden;
}

*, *::before, *::after {
  box-sizing: border-box;
}

/* Reduced motion preference */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    transition-duration: 0ms !important;
    animation-duration: 0ms !important;
  }
}
```

3. Update src/renderer/main.tsx to import the globals.css and Inter font:
```typescript
import '@fontsource/inter/400.css';
import '@fontsource/inter/600.css';
import './shared/styles/globals.css';
```
These imports must come BEFORE the App import and React rendering code. Do NOT import weight 500 — only 400 and 600 are used.

CRITICAL: Do NOT include --font-size-micro, --font-size-caption, --font-size-label, --space-3, or --space-5. These were consolidated per UI-SPEC.md Implementation Notes item 8.
  </action>
  <verify>
    <automated>cd "D:/projects/Portfolio Projects/monolith" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/shared/styles/globals.css contains "@import \"tailwindcss\""
    - src/renderer/shared/styles/globals.css contains "@theme {"
    - src/renderer/shared/styles/globals.css contains "--color-bg-base: #16161e"
    - src/renderer/shared/styles/globals.css contains "--color-bg-elevated: #1a1a2e"
    - src/renderer/shared/styles/globals.css contains "--color-bg-overlay: #1e1e30"
    - src/renderer/shared/styles/globals.css contains "--color-bg-subtle: #22223a"
    - src/renderer/shared/styles/globals.css contains "--color-accent: #6366f1"
    - src/renderer/shared/styles/globals.css contains "--color-accent-hover: #818cf8"
    - src/renderer/shared/styles/globals.css contains "--color-text-primary: #e8e8f0"
    - src/renderer/shared/styles/globals.css contains "--color-text-secondary: #9494a8"
    - src/renderer/shared/styles/globals.css contains "--color-text-muted: #5a5a72"
    - src/renderer/shared/styles/globals.css contains "--color-border: rgba(255, 255, 255, 0.07)"
    - src/renderer/shared/styles/globals.css contains "--font-size-small: 11px"
    - src/renderer/shared/styles/globals.css contains "--font-size-body: 13px"
    - src/renderer/shared/styles/globals.css contains "--font-size-heading: 15px"
    - src/renderer/shared/styles/globals.css contains "--font-size-display: 18px"
    - src/renderer/shared/styles/globals.css contains "--space-1: 4px"
    - src/renderer/shared/styles/globals.css contains "--space-2: 8px"
    - src/renderer/shared/styles/globals.css contains "--space-4: 16px"
    - src/renderer/shared/styles/globals.css contains "--sidebar-width: 52px"
    - src/renderer/shared/styles/globals.css contains "--drag-region-height: 36px"
    - src/renderer/shared/styles/globals.css contains "--duration-fast: 80ms"
    - src/renderer/shared/styles/globals.css contains "--duration-normal: 150ms"
    - src/renderer/shared/styles/globals.css contains "user-select: none"
    - src/renderer/shared/styles/globals.css contains "-webkit-font-smoothing: antialiased"
    - src/renderer/shared/styles/globals.css contains "prefers-reduced-motion"
    - src/renderer/shared/styles/globals.css does NOT contain "--font-size-micro"
    - src/renderer/shared/styles/globals.css does NOT contain "--font-size-caption"
    - src/renderer/shared/styles/globals.css does NOT contain "--font-size-label"
    - src/renderer/shared/styles/globals.css does NOT contain "--space-3"
    - src/renderer/shared/styles/globals.css does NOT contain "--space-5"
    - src/renderer/main.tsx contains "@fontsource/inter/400.css"
    - src/renderer/main.tsx contains "@fontsource/inter/600.css"
    - src/renderer/main.tsx does NOT contain "@fontsource/inter/500.css"
    - src/renderer/main.tsx contains "globals.css"
  </acceptance_criteria>
  <done>globals.css defines the complete design token system using Tailwind v4 @theme. Inter font loaded locally at weights 400 and 600 only. All token values match UI-SPEC.md exactly. No hardcoded values outside globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Build shell layout — WindowChrome, Sidebar, ModuleHeader, and App root with state-based navigation</name>
  <files>
    src/renderer/shell/WindowChrome.tsx,
    src/renderer/shell/Sidebar.tsx,
    src/renderer/shell/ModuleHeader.tsx,
    src/renderer/App.tsx
  </files>
  <read_first>
    src/renderer/shared/styles/globals.css (token names from Task 1),
    .planning/phases/01-foundation/01-UI-SPEC.md (Component Contracts section — WindowChrome, Sidebar, ModuleHeader specs),
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: Keyboard Router props interface, Code Examples: Shell App Root, Sidebar Component),
    .planning/phases/01-foundation/01-CONTEXT.md (shell layout locked decisions)
  </read_first>
  <action>
1. Create src/renderer/shell/WindowChrome.tsx — frameless window drag region and controls:

   - Full-width div at top, height: var(--drag-region-height) = 36px
   - Apply CSS `-webkit-app-region: drag` to the container
   - Center the text "Monolith" in the drag region: font-size var(--font-size-body) (13px), weight 400, color var(--color-text-secondary)
   - Right-aligned window control buttons, each `-webkit-app-region: no-drag`:
     - Close button: 12px circle, background #ef4444 (--color-destructive)
     - Minimize button: 12px circle, background #f59e0b
     - Maximize button: 12px circle, background #22c55e
     - 8px gap between circles
   - On macOS (detect via navigator.platform or import platform info): hide custom controls and just render drag region (native traffic lights handle window controls)
   - Window control click handlers call Electron IPC:
     ```typescript
     // These need to be exposed via preload or use a separate channel
     // For Phase 1: use ipcRenderer.send for window controls
     ```
   - Add the window control IPC channels to the preload bridge:
     Add to src/shared/ipc-types.ts:
     ```typescript
     export interface WindowAPI {
       minimize: () => void;
       maximize: () => void;
       close: () => void;
     }
     ```
     Add to API interface: `window: WindowAPI;`
     Add to preload:
     ```typescript
     window: {
       minimize: () => ipcRenderer.send('window:minimize'),
       maximize: () => ipcRenderer.send('window:maximize'),
       close: () => ipcRenderer.send('window:close'),
     }
     ```
     Add IPC listeners in src/main/index.ts:
     ```typescript
     ipcMain.on('window:minimize', () => win?.minimize());
     ipcMain.on('window:maximize', () => { win?.isMaximized() ? win.unmaximize() : win?.maximize(); });
     ipcMain.on('window:close', () => win?.close());
     ```

2. Create src/renderer/shell/Sidebar.tsx — icon-only navigation:

   - Width: var(--sidebar-width) = 52px
   - Background: var(--color-bg-elevated)
   - Right border: 1px solid var(--color-border)
   - Full viewport height minus drag region
   - Flex column layout
   - Four navigation icon buttons at top:
     - Dashboard: LayoutDashboard from lucide-react
     - Habits: Activity from lucide-react
     - Planner: CheckSquare from lucide-react
     - Expenses: Wallet from lucide-react
   - Settings icon at bottom via margin-top: auto:
     - Settings: Settings from lucide-react
   - Each icon button:
     - Height: 44px, Width: 44px (centered in 52px with 4px side padding)
     - Icon size: 18px, strokeWidth: 1.5
     - Vertical spacing between icons: var(--space-2) = 8px
     - Rest state: icon color var(--color-text-muted), no background
     - Hover state: icon color var(--color-text-secondary), background var(--color-bg-subtle), border-radius var(--radius-md) = 6px
     - Active state: icon color var(--color-accent), background var(--color-accent-subtle), left edge 2px solid var(--color-accent), border-radius 0 6px 6px 0
     - Transition: background and color with var(--duration-fast) = 80ms ease-out
   - Browser native `title` attribute for tooltip
   - `aria-label` on each button = module name
   - `aria-current="page"` on the active button

   Props interface:
   ```typescript
   import type { ModuleId } from '../App';
   interface SidebarProps {
     activeModule: ModuleId;
     onNavigate: (id: ModuleId) => void;
   }
   ```

3. Create src/renderer/shell/ModuleHeader.tsx:

   - Height: 40px
   - Background: var(--color-bg-base)
   - Bottom border: 1px solid var(--color-border)
   - Left content: module display name at var(--font-size-heading) = 15px, weight 600, color var(--color-text-primary)
   - Left padding: var(--space-4) = 16px
   - Right padding: var(--space-4) = 16px
   - Vertical alignment: center (flexbox)
   - Module name mapping: dashboard -> "Dashboard", habits -> "Habits", planner -> "Planner", expenses -> "Expenses", settings -> "Settings"

   Props interface:
   ```typescript
   interface ModuleHeaderProps {
     moduleId: ModuleId;
   }
   ```

4. Update src/renderer/App.tsx — root component with state-based navigation:

   ```typescript
   import { useState } from 'react';
   import { Sidebar } from './shell/Sidebar';
   import { WindowChrome } from './shell/WindowChrome';
   import { ModuleHeader } from './shell/ModuleHeader';

   export type ModuleId = 'dashboard' | 'habits' | 'planner' | 'expenses' | 'settings';

   export default function App() {
     const [activeModule, setActiveModule] = useState<ModuleId>('dashboard');

     return (
       <div style={{ display: 'flex', flexDirection: 'column', height: '100vh', overflow: 'hidden' }}>
         <WindowChrome />
         <div style={{ display: 'flex', flex: 1, overflow: 'hidden' }}>
           <Sidebar activeModule={activeModule} onNavigate={setActiveModule} />
           <div style={{ flex: 1, display: 'flex', flexDirection: 'column', overflow: 'hidden' }}>
             <ModuleHeader moduleId={activeModule} />
             <main style={{ flex: 1, overflow: 'auto', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
               {/* Phase 1 placeholder content — replaced in Phase 2 */}
               <span style={{
                 color: 'var(--color-text-muted)',
                 fontSize: 'var(--font-size-body)',
               }}>
                 {activeModule === 'dashboard' ? 'Dashboard' :
                  activeModule === 'settings' ? null :
                  'Coming in Phase 2'}
               </span>
             </main>
           </div>
         </div>
       </div>
     );
   }
   ```

   Note: Settings view placeholder for now — Plan 03 will render the actual SettingsView when activeModule === 'settings'. The keyboard router will be added in Plan 04.

CRITICAL: All colors, sizes, spacing values MUST use var(--token-name) references — never hardcode hex values, pixel sizes, or spacing numbers directly in component files. The only exception is the window control button colors (#ef4444, #f59e0b, #22c55e) which are semantic one-offs defined in UI-SPEC (not tokens).
  </action>
  <verify>
    <automated>cd "D:/projects/Portfolio Projects/monolith" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/shell/WindowChrome.tsx contains "webkit-app-region"
    - src/renderer/shell/WindowChrome.tsx contains "drag"
    - src/renderer/shell/WindowChrome.tsx contains "no-drag"
    - src/renderer/shell/WindowChrome.tsx contains "Monolith"
    - src/renderer/shell/WindowChrome.tsx contains "var(--drag-region-height)"
    - src/renderer/shell/WindowChrome.tsx contains "window.api.window"
    - src/renderer/shell/Sidebar.tsx contains "var(--sidebar-width)"
    - src/renderer/shell/Sidebar.tsx contains "var(--color-bg-elevated)"
    - src/renderer/shell/Sidebar.tsx contains "var(--color-accent)"
    - src/renderer/shell/Sidebar.tsx contains "var(--color-accent-subtle)"
    - src/renderer/shell/Sidebar.tsx contains "var(--color-text-muted)"
    - src/renderer/shell/Sidebar.tsx contains "import.*lucide-react"
    - src/renderer/shell/Sidebar.tsx contains "LayoutDashboard"
    - src/renderer/shell/Sidebar.tsx contains "Activity"
    - src/renderer/shell/Sidebar.tsx contains "CheckSquare"
    - src/renderer/shell/Sidebar.tsx contains "Wallet"
    - src/renderer/shell/Sidebar.tsx contains "Settings"
    - src/renderer/shell/Sidebar.tsx contains "aria-label"
    - src/renderer/shell/Sidebar.tsx contains "aria-current"
    - src/renderer/shell/Sidebar.tsx contains "title="
    - src/renderer/shell/ModuleHeader.tsx contains "var(--font-size-heading)"
    - src/renderer/shell/ModuleHeader.tsx contains "var(--color-text-primary)"
    - src/renderer/shell/ModuleHeader.tsx contains "var(--color-border)"
    - src/renderer/App.tsx contains "export type ModuleId"
    - src/renderer/App.tsx contains "activeModule"
    - src/renderer/App.tsx contains "setActiveModule"
    - src/renderer/App.tsx contains "<Sidebar"
    - src/renderer/App.tsx contains "<WindowChrome"
    - src/renderer/App.tsx contains "<ModuleHeader"
    - src/renderer/App.tsx contains "'dashboard'"
    - src/renderer/App.tsx contains "'habits'"
    - src/renderer/App.tsx contains "'planner'"
    - src/renderer/App.tsx contains "'expenses'"
    - src/renderer/App.tsx contains "'settings'"
    - src/shared/ipc-types.ts contains "WindowAPI"
    - TypeScript compilation succeeds (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>Shell layout complete — frameless window with drag region, 52px icon-only sidebar with Lucide icons (5 navigation targets), header bar with module name, state-based module switching. All visual values reference CSS custom properties. No hardcoded hex in component files except window control button colors.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All component files use `var(--token-name)` for colors, sizes, spacing — grep for hardcoded hex should only find window control colors
- Sidebar renders 5 icon buttons with proper active/hover states
- WindowChrome has drag region and no-drag controls
- Module switching is state-based (no router, no page reload)
- globals.css has exactly 4 font sizes, 5 spacing values, full color palette
</verification>

<success_criteria>
The app renders with the complete dark, dense, handcrafted aesthetic. Sidebar navigation switches between module placeholders instantly. The frameless window has a draggable title region. All visual values are tokenized in CSS custom properties.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
