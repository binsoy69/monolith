---
phase: 02-module-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/renderer/habits/HabitForm.tsx
  - src/renderer/habits/DayPicker.tsx
  - src/renderer/habits/HabitContextMenu.tsx
  - src/renderer/habits/ArchiveConfirmation.tsx
  - src/renderer/habits/ArchivedHabitsView.tsx
  - src/renderer/habits/HabitsView.tsx
  - src/renderer/shell/KeyboardRouter.tsx
autonomous: true
requirements:
  - HAB-01

must_haves:
  truths:
    - "User can create a habit with a name and day-of-week schedule via inline form"
    - "User can edit a habit name and schedule via the same inline form (pre-filled)"
    - "User can archive a habit via right-click context menu with inline confirmation"
    - "Archived habits are hidden from today view"
    - "User can toggle between active and archived habit views"
    - "Pressing N in habits module opens the create form"
    - "Schedule picker shows Mon-Sun checkbox row"
  artifacts:
    - path: "src/renderer/habits/HabitForm.tsx"
      provides: "Inline expandable create/edit form with name input and day picker"
      exports: ["HabitForm"]
    - path: "src/renderer/habits/DayPicker.tsx"
      provides: "Row of 7 day checkboxes (Mon-Sun)"
      exports: ["DayPicker"]
    - path: "src/renderer/habits/HabitContextMenu.tsx"
      provides: "Right-click menu with Edit and Archive options"
      exports: ["HabitContextMenu"]
    - path: "src/renderer/habits/ArchivedHabitsView.tsx"
      provides: "List of archived habits"
      exports: ["ArchivedHabitsView"]
  key_links:
    - from: "src/renderer/habits/HabitsView.tsx"
      to: "src/renderer/habits/HabitForm.tsx"
      via: "conditional render when showForm state is true"
      pattern: "HabitForm"
    - from: "src/renderer/habits/HabitForm.tsx"
      to: "src/renderer/habits/habits-store.ts"
      via: "createHabit or updateHabit calls"
      pattern: "(createHabit|updateHabit)"
    - from: "src/renderer/habits/HabitContextMenu.tsx"
      to: "src/renderer/shared/ContextMenu.tsx"
      via: "shared context menu component"
      pattern: "ContextMenu"
---

<objective>
Add habit create, edit, and archive flows to complete the HAB-01 requirement. Users can manage their habit list through an inline form and right-click context menu.

Purpose: Completes the habit module's full CRUD lifecycle — create with schedule, edit, and archive with confirmation.
Output: HabitForm, DayPicker, context menu, archive confirmation, archived view, and "N" keyboard shortcut.
</objective>

<execution_context>
@C:/Users/binsl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/binsl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-module-core/02-CONTEXT.md
@.planning/phases/02-module-core/02-UI-SPEC.md
@.planning/phases/02-module-core/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 outputs -->

From src/renderer/habits/habits-store.ts:
```typescript
interface HabitsStore {
  habits: HabitWithToday[]
  isLoaded: boolean
  showArchived: boolean
  load: (date: string) => Promise<void>
  toggleComplete: (habitId: string, date: string) => Promise<void>
  createHabit: (data: { name: string; daysOfWeek: string }) => Promise<void>
  updateHabit: (id: string, data: { name?: string; daysOfWeek?: string }) => Promise<void>
  archiveHabit: (id: string) => Promise<void>
  setShowArchived: (show: boolean) => void
}
export const useHabitsStore: UseBoundStore<StoreApi<HabitsStore>>
```

From src/renderer/shared/ContextMenu.tsx:
```typescript
interface ContextMenuProps {
  items: Array<{ label: string; onClick: () => void; destructive?: boolean }>
  position: { x: number; y: number }
  onClose: () => void
}
```

From src/renderer/shared/useContextMenu.ts:
```typescript
function useContextMenu(): {
  contextMenu: { visible: boolean; position: { x: number; y: number }; items: ContextMenuItem[] }
  showContextMenu: (e: React.MouseEvent, items: ContextMenuItem[]) => void
  hideContextMenu: () => void
}
```

From src/renderer/shell/ModuleHeader.tsx:
```typescript
interface ModuleHeaderProps {
  moduleId: ModuleId
  left?: React.ReactNode
  right?: React.ReactNode
}
```

From src/renderer/shell/KeyboardRouter.tsx:
```typescript
// Currently handles Alt+1-4, Escape, and ?
// Needs addition of "N" key handler (when not editing)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HabitForm, DayPicker, and keyboard shortcut</name>
  <files>
    src/renderer/habits/HabitForm.tsx,
    src/renderer/habits/DayPicker.tsx,
    src/renderer/habits/HabitsView.tsx,
    src/renderer/shell/KeyboardRouter.tsx
  </files>
  <read_first>
    src/renderer/habits/HabitsView.tsx,
    src/renderer/habits/habits-store.ts,
    src/renderer/shell/ModuleHeader.tsx,
    src/renderer/shell/KeyboardRouter.tsx,
    src/renderer/App.tsx,
    src/renderer/shared/styles/globals.css,
    .planning/phases/02-module-core/02-UI-SPEC.md,
    .planning/phases/02-module-core/02-CONTEXT.md
  </read_first>
  <action>
    **Step 1 — Create `src/renderer/habits/DayPicker.tsx`:**
    Props: `{ selectedDays: string; onChange: (daysOfWeek: string) => void }`.
    - `selectedDays` is a 7-char bitmask string where index 0=Sun, 1=Mon, ..., 6=Sat
    - Render a row of 7 toggle buttons, ordered Mon-Sun visually (indices 1,2,3,4,5,6,0)
    - Labels: "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"
    - Each button: 36px width, 28px height, border-radius `var(--radius-sm)`, font-size `var(--font-size-small)`
    - Selected: background `var(--color-accent)`, color white
    - Unselected: background transparent, border 1px solid `var(--color-border)`, color `var(--color-text-secondary)`
    - Hover (unselected): background `var(--color-bg-subtle)`
    - Gap between buttons: `var(--space-1)`
    - On click, toggle the corresponding bit in the bitmask string and call onChange

    **Step 2 — Create `src/renderer/habits/HabitForm.tsx`:**
    Props: `{ mode: 'create' | 'edit'; initialName?: string; initialDaysOfWeek?: string; onSubmit: (data: { name: string; daysOfWeek: string }) => void; onCancel: () => void }`.
    - Inline form that appears at the top of the habit card list (per CONTEXT.md: "inline expandable form at the top of the card list")
    - Background: `var(--color-bg-elevated)`, border: 1px solid `var(--color-border)`, border-radius `var(--radius-md)`, padding `var(--space-4)`
    - Name input: full width, height 32px, bg `var(--color-bg-base)`, border `var(--color-border)`, border-radius `var(--radius-sm)`, font-size body, placeholder "Habit name"
    - Focus: border changes to `var(--color-border-focused)`
    - Below input: DayPicker component. Default for new habits: '1111111' (every day)
    - Below day picker: row with Cancel button (text-secondary color, no bg) and Submit button (bg accent, color white, border-radius radius-sm). Create mode: "Create Habit". Edit mode: "Save Changes".
    - Enter key on name input submits if name is non-empty
    - Escape cancels the form
    - Animate open: max-height transition 150ms ease-out
    - When form is open, rest of habit list dims to opacity 0.6 (per UI-SPEC: "rest of list dims to opacity 0.6")
    - Auto-focus the name input on mount

    **Step 3 — Update `src/renderer/habits/HabitsView.tsx`:**
    Add state: `showForm: boolean`, `editingHabit: HabitWithToday | null`.
    - When `showForm` is true and `editingHabit` is null: render HabitForm in create mode at top of card list
    - When `showForm` is true and `editingHabit` is set: render HabitForm in edit mode with pre-filled values
    - On form submit (create): call `habitsStore.createHabit(data)`, close form
    - On form submit (edit): call `habitsStore.updateHabit(editingHabit.id, data)`, close form
    - Pass ModuleHeader right slot:
      - "Show Archived" / "Show Active" toggle text button (font-size small, color text-secondary, cursor pointer)
      - "+ New Habit" button (font-size small, color accent, cursor pointer). Clicking opens HabitForm in create mode.
    - When showArchived is true, render ArchivedHabitsView (placeholder div for now, implemented in Task 2)
    - When form is open, dim the card list to opacity 0.6

    **Step 4 — Update `src/renderer/shell/KeyboardRouter.tsx`:**
    Add new props: `activeModule: ModuleId` and `onNewItem: () => void`.
    In the `!isEditing` block, add handler for "n" or "N" key:
    ```typescript
    if (e.key === 'n' || e.key === 'N') {
      e.preventDefault();
      onNewItem();
      return;
    }
    ```
    Update App.tsx to pass `activeModule` and a `handleNewItem` callback. The callback should:
    - If activeModule === 'habits': trigger form open in HabitsView
    - If activeModule === 'planner': focus quick-add input (will be wired in plan 04)
    - If activeModule === 'expenses': open expense modal (will be wired in plan 06)

    For now, use a simple approach: expose a `triggerNewItem` function from HabitsView via a ref or a small event emitter. Or simpler: add a `newItemTrigger` state in App.tsx that increments, pass to HabitsView as prop, HabitsView watches for changes and opens form.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/renderer/habits/HabitForm.tsx','src/renderer/habits/DayPicker.tsx'].forEach(f => { if(!fs.existsSync(f)) { console.error('MISSING: '+f); process.exit(1); } const c=fs.readFileSync(f,'utf8'); if(f.includes('DayPicker') && !c.includes('Mon')) { console.error('DayPicker missing day labels'); process.exit(1); } }); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/habits/DayPicker.tsx contains labels "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"
    - src/renderer/habits/DayPicker.tsx contains `var(--color-accent)` for selected state
    - src/renderer/habits/HabitForm.tsx contains props `mode: 'create' | 'edit'`
    - src/renderer/habits/HabitForm.tsx contains `var(--color-bg-elevated)` for form background
    - src/renderer/habits/HabitForm.tsx contains `var(--color-border-focused)` for input focus
    - src/renderer/habits/HabitForm.tsx contains `DayPicker` import
    - src/renderer/habits/HabitsView.tsx contains `+ New Habit` button text
    - src/renderer/habits/HabitsView.tsx contains `Show Archived` text
    - src/renderer/habits/HabitsView.tsx contains `HabitForm` import
    - src/renderer/habits/HabitsView.tsx contains `createHabit` call
    - src/renderer/habits/HabitsView.tsx contains `updateHabit` call
    - src/renderer/habits/HabitsView.tsx contains `opacity: 0.6` for dimmed list during form edit
    - src/renderer/shell/KeyboardRouter.tsx contains handler for key 'n' or 'N'
    - src/renderer/shell/KeyboardRouter.tsx contains `onNewItem` in props
  </acceptance_criteria>
  <done>HabitForm opens inline at top of card list for create (empty) and edit (pre-filled). DayPicker shows Mon-Sun toggles. "+ New Habit" button in ModuleHeader right slot. "N" keyboard shortcut opens create form. Form submission creates/updates habit via store.</done>
</task>

<task type="auto">
  <name>Task 2: Context menu, archive confirmation, and archived view</name>
  <files>
    src/renderer/habits/HabitContextMenu.tsx,
    src/renderer/habits/ArchiveConfirmation.tsx,
    src/renderer/habits/ArchivedHabitsView.tsx,
    src/renderer/habits/HabitsView.tsx,
    src/renderer/habits/HabitCard.tsx
  </files>
  <read_first>
    src/renderer/habits/HabitsView.tsx,
    src/renderer/habits/HabitCard.tsx,
    src/renderer/habits/habits-store.ts,
    src/renderer/shared/ContextMenu.tsx,
    src/renderer/shared/useContextMenu.ts,
    .planning/phases/02-module-core/02-UI-SPEC.md,
    .planning/phases/02-module-core/02-CONTEXT.md
  </read_first>
  <action>
    **Step 1 — Create `src/renderer/habits/ArchiveConfirmation.tsx`:**
    Props: `{ habitName: string; onConfirm: () => void; onCancel: () => void }`.
    - Inline prompt that replaces the habit card temporarily (per CONTEXT.md and UI-SPEC)
    - Same dimensions as a HabitCard (bg-elevated, border, radius-md, same padding)
    - Text: `Archive "{habitName}"? This habit will be hidden from today's view.` (font-size body)
    - Two buttons in a row:
      - "Archive" — color `var(--color-destructive)`, transparent background
      - "Keep Habit" — color `var(--color-text-secondary)`, transparent background
    - Both buttons: font-size small, cursor pointer, padding space-1 space-2
    - Escape key or "Keep Habit" click calls onCancel

    **Step 2 — Create `src/renderer/habits/ArchivedHabitsView.tsx`:**
    - Reads archived habits: in HabitsView, when showArchived is true, call `window.api.habits.listArchived()` via a useEffect (or add loadArchived to the store)
    - List of archived habit names at font-size body, color text-secondary
    - Each row shows habit name and days-of-week text (e.g., "Mon, Wed, Fri")
    - No checkboxes, no streaks — archived habits are display-only
    - If empty: centered text "No archived habits" at font-size body, color text-muted
    - No context menu on archived habits (archive is one-way for now; unarchive deferred)

    **Step 3 — Wire context menu in `src/renderer/habits/HabitsView.tsx`:**
    Use the `useContextMenu` hook. When a HabitCard's `onContextMenu` fires:
    - Show context menu with two items:
      - { label: "Edit", onClick: () => { open HabitForm in edit mode for this habit } }
      - { label: "Archive", onClick: () => { show archive confirmation for this habit }, destructive: true }
    - Render the ContextMenu component from the hook's state

    **Step 4 — Wire archive confirmation in HabitsView:**
    State: `archivingHabit: HabitWithToday | null`.
    When archive is clicked from context menu, set `archivingHabit`.
    Render ArchiveConfirmation in place of that habit's HabitCard (swap in the list).
    On confirm: call `habitsStore.archiveHabit(habit.id)`, clear archivingHabit state.
    On cancel: clear archivingHabit state, card returns to normal.

    **Step 5 — Update `src/renderer/habits/HabitCard.tsx`:**
    Ensure the `onContextMenu` prop is properly wired to `e.preventDefault()` + parent handler.
    Add tooltip for unscheduled habits: title attribute "Not scheduled for today" (per UI-SPEC copywriting).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/renderer/habits/ArchiveConfirmation.tsx','src/renderer/habits/ArchivedHabitsView.tsx'].forEach(f => { if(!fs.existsSync(f)) { console.error('MISSING: '+f); process.exit(1); } }); const hv=fs.readFileSync('src/renderer/habits/HabitsView.tsx','utf8'); if(!hv.includes('useContextMenu')) { console.error('HabitsView missing useContextMenu'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/habits/ArchiveConfirmation.tsx contains text "Archive" as button label
    - src/renderer/habits/ArchiveConfirmation.tsx contains text "Keep Habit" as cancel button
    - src/renderer/habits/ArchiveConfirmation.tsx contains `var(--color-destructive)` on archive button
    - src/renderer/habits/ArchivedHabitsView.tsx contains "No archived habits" empty state text
    - src/renderer/habits/HabitsView.tsx contains `useContextMenu` hook usage
    - src/renderer/habits/HabitsView.tsx contains context menu item with label "Edit"
    - src/renderer/habits/HabitsView.tsx contains context menu item with label "Archive"
    - src/renderer/habits/HabitsView.tsx contains `archiveHabit` call
    - src/renderer/habits/HabitCard.tsx contains `onContextMenu` prop handling
    - src/renderer/habits/HabitCard.tsx contains "Not scheduled for today" tooltip text
  </acceptance_criteria>
  <done>Right-click on a habit card shows Edit/Archive context menu. Clicking Edit opens HabitForm pre-filled. Clicking Archive shows inline confirmation. Confirming archive removes habit with optimistic update. Toggling "Show Archived" displays archived habits list. Full HAB-01 requirement complete.</done>
</task>

</tasks>

<verification>
- Right-click on any habit card shows context menu with "Edit" and "Archive" options
- Editing opens pre-filled inline form; saving updates the habit
- Archive shows inline confirmation; confirming hides the habit
- "Show Archived" toggle shows archived habits (or empty state if none)
- "N" key opens create form from anywhere in habits module
- "+ New Habit" button opens create form
</verification>

<success_criteria>
1. Full CRUD lifecycle for habits: create with schedule, edit name/schedule, archive with confirmation
2. Context menu renders via portal (not clipped by scroll containers)
3. Archive confirmation replaces card inline (not a modal)
4. "N" keyboard shortcut triggers new item action
5. Archived view shows archived habits or empty state
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-core/02-02-SUMMARY.md`
</output>
