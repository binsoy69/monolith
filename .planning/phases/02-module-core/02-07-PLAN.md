---
phase: 02-module-core
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/shared/styles/globals.css
  - src/renderer/habits/HabitsView.tsx
  - src/renderer/habits/HabitCard.tsx
  - src/renderer/habits/HabitForm.tsx
  - src/renderer/habits/HabitProgressBar.tsx
  - src/renderer/habits/ArchivedHabitsView.tsx
  - src/renderer/expenses/WalletPanel.tsx
  - src/renderer/expenses/WalletCard.tsx
  - src/renderer/expenses/CategoryManageView.tsx
  - src/renderer/expenses/ExpensesView.tsx
autonomous: true
gap_closure: true
requirements: [HAB-01, HAB-02, HAB-03, HAB-05, EXP-06, EXP-07, EXP-09]

must_haves:
  truths:
    - "All small text across the app is legible (12px minimum, brighter muted color)"
    - "Archived habits show formatted schedule days clearly visible"
    - "Active habit cards show schedule days as subtitle"
    - "Habit header buttons are clearly recognizable and properly sized"
    - "Wallet add form inputs appear near the button, not at bottom"
    - "Wallet card action icons are properly sized with adequate click targets"
    - "Users can add new categories from the manage categories section"
    - "Manage categories toggle text is clearly visible"
  artifacts:
    - path: "src/renderer/shared/styles/globals.css"
      provides: "Updated design tokens"
      contains: "--font-size-small: 12px"
    - path: "src/renderer/expenses/CategoryManageView.tsx"
      provides: "Add category functionality"
      contains: "onCreate"
  key_links:
    - from: "src/renderer/expenses/ExpensesView.tsx"
      to: "CategoryManageView"
      via: "onCreate prop"
      pattern: "onCreate.*createCategory"
---

<objective>
Fix all cosmetic and small UX issues identified in UAT tests 2, 5, 13, 14, 18.

Purpose: Address 7 of 9 UAT gaps — global design token adjustments (font size, muted color contrast), habit text visibility, wallet form positioning, wallet icon sizing, and category creation capability.
Output: All cosmetic UAT issues resolved; category creation in manage view working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-core/02-UAT.md
</context>

<interfaces>
<!-- Key types the executor needs -->

From src/renderer/expenses/CategoryManageView.tsx:
```typescript
interface CategoryManageViewProps {
  categories: Category[]
  onUpdate: (id: string, data: { name?: string; color?: string }) => void
  onDelete: (id: string) => Promise<boolean>
  // MISSING: onCreate — needs to be added
}
```

From src/renderer/expenses/InlineCategoryForm.tsx:
```typescript
interface InlineCategoryFormProps {
  onSave: (data: { name: string; color: string }) => void
  onCancel: () => void
}
```

From src/renderer/expenses/ExpensesView.tsx:
```typescript
// createCategory is available in useExpensesStore but not passed to CategoryManageView
const { createCategory } = useExpensesStore()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Global design tokens + habits text visibility fixes</name>
  <files>
    src/renderer/shared/styles/globals.css,
    src/renderer/habits/HabitsView.tsx,
    src/renderer/habits/HabitCard.tsx,
    src/renderer/habits/HabitForm.tsx,
    src/renderer/habits/HabitProgressBar.tsx,
    src/renderer/habits/ArchivedHabitsView.tsx
  </files>
  <action>
    1. In globals.css, update two design tokens:
       - Change `--font-size-small: 11px` to `--font-size-small: 12px`
       - Change `--color-text-muted: #5a5a72` to `--color-text-muted: #7a7a92`

    2. In HabitsView.tsx, update header action buttons ("+ New Habit", "Show Archived"):
       - Change fontSize from `var(--font-size-small)` to `var(--font-size-body)` on both buttons (lines 142, 157)

    3. In HabitCard.tsx, add schedule days subtitle below the habit name:
       - Import or inline the formatDaysOfWeek function (same logic as ArchivedHabitsView: index 0=Sun..6=Sat, display Mon-Sun order, "Every day" if all 7)
       - After the habit name span, add a new span showing `formatDaysOfWeek(habit.daysOfWeek)` with fontSize: var(--font-size-small), color: var(--color-text-secondary)
       - Wrap name + days in a flex-column div with gap: 2px so they stack vertically
       - The HabitCard content area (name + optional days) should be a column, the outer card row stays horizontal

    4. In HabitProgressBar.tsx:
       - Change the "completed" text color from var(--color-text-muted) to var(--color-text-secondary) so it's more legible (the number already uses accent color, just the "/N completed" text)

    5. In ArchivedHabitsView.tsx:
       - Change days span color from `var(--color-text-muted)` to `var(--color-text-secondary)` (line 109)
       - Change days span fontSize from `var(--font-size-small)` to `var(--font-size-body)` (line 108)

    6. In HabitForm.tsx:
       - Find the submit button ("Create Habit" / "Save Changes") and change its fontSize from var(--font-size-small) to var(--font-size-body) if it uses font-size-small
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - --font-size-small is 12px, --color-text-muted is #7a7a92
    - Habit header buttons use font-size-body (13px)
    - Active habit cards show schedule days as subtitle text
    - Archived habit days text uses color-text-secondary at font-size-body
    - Progress bar text uses color-text-secondary
  </done>
</task>

<task type="auto">
  <name>Task 2: Wallet, category, and expenses cosmetic fixes</name>
  <files>
    src/renderer/expenses/WalletPanel.tsx,
    src/renderer/expenses/WalletCard.tsx,
    src/renderer/expenses/CategoryManageView.tsx,
    src/renderer/expenses/ExpensesView.tsx
  </files>
  <action>
    1. In WalletPanel.tsx — Move add wallet form INSIDE the scrollable wallet list area:
       - Move the `{showAddForm && (...)}` block (currently at lines 153-252, AFTER the flex:1 wallet list div) to INSIDE the wallet list div (the `flex: 1, overflowY: 'auto'` container at line 90-151)
       - Place it as the FIRST child inside that container, BEFORE the wallet cards map
       - Remove the borderTop style from the form div since it will now be inside the scrollable area
       - Keep the "+ Add Wallet" button at the bottom where it is (lines 255-270)

    2. In WalletCard.tsx — Increase icon sizes and add click targets:
       - Change both Pencil and ArrowUpDown icon `size={14}` to `size={16}`
       - Add `padding: '4px'` to both button styles (replacing `padding: 0`)
       - Add `borderRadius: 'var(--radius-sm)'` to both button styles
       - Change default color from `var(--color-text-muted)` to `var(--color-text-secondary)` on both buttons
       - Update hover handlers: onMouseEnter sets color to `var(--color-text-primary)`, onMouseLeave sets back to `var(--color-text-secondary)`
       - Add hover background: onMouseEnter also sets backgroundColor to `var(--color-bg-subtle)`, onMouseLeave resets to `transparent`

    3. In CategoryManageView.tsx — Add category creation:
       - Add `onCreate?: (data: { name: string; color: string }) => void` to CategoryManageViewProps
       - Import InlineCategoryForm from './InlineCategoryForm'
       - Add `const [showAddForm, setShowAddForm] = useState(false)` state
       - After the categories list (after the closing of the categories.map div), add:
         - If showAddForm: render `<InlineCategoryForm onSave={(data) => { onCreate?.(data); setShowAddForm(false); }} onCancel={() => setShowAddForm(false)} />`
         - If not showAddForm and onCreate exists: render a "+ New Category" button with fontSize: var(--font-size-body), color: var(--color-accent), no background/border, cursor: pointer, marginTop: var(--space-2)

    4. In ExpensesView.tsx — Wire category creation + fix toggle visibility:
       - Pass `onCreate={createCategory}` prop to CategoryManageView (around line 201)
       - Change the "Manage categories" toggle button default color from `var(--color-text-muted)` to `var(--color-text-secondary)` (line 182)
       - Update onMouseLeave to also use `var(--color-text-secondary)` instead of `var(--color-text-muted)` when not active (line 193)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Wallet add form appears at top of wallet list (inside scrollable area), not at bottom
    - Wallet card icons are 16px with 4px padding and hover background
    - CategoryManageView has "+ New Category" button that opens InlineCategoryForm
    - createCategory is wired from ExpensesView to CategoryManageView via onCreate prop
    - "Manage categories" toggle text uses color-text-secondary (visible against dark bg)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- App builds: `npm run build` completes successfully
</verification>

<success_criteria>
UAT tests 2, 5, 13, 14, 18 cosmetic/major issues are resolved. All text is legible, icons are properly sized, wallet form appears in correct position, and users can add new categories.
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-core/02-07-SUMMARY.md`
</output>
