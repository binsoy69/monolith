---
phase: 04-depth-differentiators
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/repositories/PlannerRepository.ts
  - src/renderer/planner/TaskList.tsx
  - tests/planner-depth-repository.test.ts
  - tests/task-list-ordering.test.tsx
autonomous: true
gap_closure: true
requirements: [PLAN-06, PLAN-07, PLAN-08]

must_haves:
  truths:
    - "Changing a task to P1, P2, or P3 immediately moves higher-priority tasks above lower-priority tasks in today's list."
    - "Carried-forward tasks remain at the top of today's incomplete list per the locked Phase 4 behavior, while still honoring priority within that carried group."
    - "Manual drag order still persists inside the same carry/priority band and survives reload."
  artifacts:
    - path: "src/main/repositories/PlannerRepository.ts"
      provides: "Priority-aware query ordering"
      contains: "ORDER BY"
    - path: "src/renderer/planner/TaskList.tsx"
      provides: "Renderer sort parity with repository ordering"
      contains: "priority"
    - path: "tests/task-list-ordering.test.tsx"
      provides: "UI ordering regression coverage"
      contains: "TaskList"
  key_links:
    - from: "src/main/repositories/PlannerRepository.ts"
      to: "src/renderer/planner/TaskList.tsx"
      via: "shared display-order contract"
      pattern: "priority"
    - from: "src/renderer/planner/TaskList.tsx"
      to: "tests/task-list-ordering.test.tsx"
      via: "priority/carry-forward component assertions"
      pattern: "carriedFromDate"
---

<objective>
Close the remaining planner UAT gap by making task priority affect actual list order instead of only rendering a decorative badge.

Purpose: Priority is already persisted and visible, but the user cannot trust it because the ordering contract ignores it. This plan makes priority real while preserving the locked carry-forward behavior from Phase 4.
Output: A consistent planner ordering contract in repository and renderer, plus regression tests that lock the new behavior in place.
</objective>

<execution_context>
@C:/Users/binsl/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/binsl/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-depth-differentiators/04-UAT.md
@.planning/phases/04-depth-differentiators/04-CONTEXT.md
@.planning/phases/04-depth-differentiators/04-02-SUMMARY.md
@.planning/debug/planner-priority-ordering.md
</context>

<interfaces>
From [src/main/repositories/PlannerRepository.ts](/D:/projects/Portfolio%20Projects/monolith/src/main/repositories/PlannerRepository.ts):
```typescript
listForDate(date: string): Task[] {
  return this.db
    .prepare(`SELECT ${this.getSelectColumns()} FROM tasks WHERE date = ? ORDER BY completed ASC, position ASC, created_at ASC`)
    .all(date) as TaskRow[]
}
```

From [src/renderer/planner/TaskList.tsx](/D:/projects/Portfolio%20Projects/monolith/src/renderer/planner/TaskList.tsx):
```typescript
const incompleteTasks = tasks
  .filter((t) => !t.completed)
  .sort((a, b) => a.position - b.position || a.createdAt.localeCompare(b.createdAt))
```

From [src/shared/domain-types.ts](/D:/projects/Portfolio%20Projects/monolith/src/shared/domain-types.ts):
```typescript
export type TaskPriority = 0 | 1 | 2 | 3

export interface Task {
  id: string
  date: string
  completed: boolean
  position: number
  priority: TaskPriority
  carriedFromDate: string | null
  createdAt: string
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Codify the planner display-order contract in automated tests</name>
  <files>
    tests/planner-depth-repository.test.ts,
    tests/task-list-ordering.test.tsx
  </files>
  <action>
    Add regression coverage for the ordering rule the user actually expects:

    1. Repository tests must encode the query contract for today's tasks:
       - incomplete tasks first
       - carried-forward tasks before same-day tasks
       - within each carry bucket, priority order `P1 -> P2 -> P3 -> none`
       - within the same carry/priority bucket, `position` then `createdAt`
       - completed tasks still render below incomplete tasks
    2. Add a renderer test for `TaskList` that mirrors the same ordering against in-memory tasks so priority changes take effect immediately without a reload.
    3. Include at least one drag-order regression case that proves manual reorder remains meaningful within a shared carry/priority band.
    4. Do not redefine the carry-forward rule: the locked decision from Phase 4 still requires carried tasks to stay at the top.
  </action>
  <verify>
    <automated>rg -n "carriedFromDate|priority|position|createdAt|TaskList" tests/planner-depth-repository.test.ts tests/task-list-ordering.test.tsx</automated>
  </verify>
  <done>
    Both repository and renderer tests explicitly describe the new ordering contract, including the interaction between carry-forward, priority, and manual position.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement priority-aware ordering in repository and TaskList</name>
  <files>
    src/main/repositories/PlannerRepository.ts,
    src/renderer/planner/TaskList.tsx,
    tests/planner-depth-repository.test.ts,
    tests/task-list-ordering.test.tsx
  </files>
  <behavior>
    - Test 1: A carried P1 task sorts above carried P2/P3/none tasks, and all carried tasks still sort above same-day incomplete tasks.
    - Test 2: Tasks with the same carry/priority band keep manual `position` order and use `createdAt` as the tie-breaker.
    - Test 3: Updating a task's priority causes `TaskList` to render the new order immediately without requiring a manual refresh.
  </behavior>
  <action>
    Apply the same ordering rule in both the main-process query and the renderer:

    1. Update `PlannerRepository.listForDate()` to sort incomplete tasks by carry bucket first, then priority, then `position`, then `created_at`. Keep completed tasks after incomplete tasks.
    2. Mirror the exact same comparator in `TaskList.tsx` so optimistic state and persisted data present the same order.
    3. Keep manual drag reorder meaningful only within the same carry/priority band. If a drag would try to move a lower-priority task above a higher-priority band, no-op the reorder callback instead of persisting misleading positions.
    4. Do not change the existing context-menu shape, priority badge UI, or completed-task non-draggable behavior.
  </action>
  <verify>
    <automated>npx vitest run tests/planner-depth-repository.test.ts tests/task-list-ordering.test.tsx</automated>
  </verify>
  <done>
    Planner priority is no longer decorative: it changes the actual display order immediately, persists after reload, and remains compatible with carry-forward and manual drag ordering.
  </done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/planner-depth-repository.test.ts tests/task-list-ordering.test.tsx`.
Run `npm run build`.
</verification>

<success_criteria>
Setting P1/P2/P3 produces a list order the user can trust: higher-priority tasks surface first, carried tasks stay at the top, and manual reordering still works inside each priority band.
</success_criteria>

<output>
After completion, create `.planning/phases/04-depth-differentiators/04-05-SUMMARY.md`
</output>
