---
phase: 04-depth-differentiators
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/expenses/ExpenseAnalyticsSection.tsx
  - src/renderer/expenses/ExpensesView.tsx
  - tests/expense-analytics-section.test.tsx
autonomous: true
gap_closure: true
requirements: [EXP-04, EXP-05, EXP-10]

must_haves:
  truths:
    - "The Expenses view shows a clearly discoverable analytics summary while still loading collapsed by default."
    - "Before expansion, the user can already see that monthly totals and charts exist because the collapsed surface shows visible summary copy."
    - "Expanding the analytics surface still reveals the existing monthly total header, donut chart, and 3/6/12 month trend controls above the expense list."
  artifacts:
    - path: "src/renderer/expenses/ExpenseAnalyticsSection.tsx"
      provides: "Collapsed analytics summary CTA with accessible expand/collapse state"
      contains: "aria-expanded"
    - path: "tests/expense-analytics-section.test.tsx"
      provides: "Discoverability regression coverage"
      contains: "Monthly analytics"
  key_links:
    - from: "src/renderer/expenses/ExpensesView.tsx"
      to: "src/renderer/expenses/ExpenseAnalyticsSection.tsx"
      via: "collapsed-by-default analytics wrapper"
      pattern: "showAnalytics"
    - from: "src/renderer/expenses/ExpenseAnalyticsSection.tsx"
      to: "tests/expense-analytics-section.test.tsx"
      via: "accessible collapsed summary contract"
      pattern: "aria-expanded"
---

<objective>
Close the remaining expense UAT gap by making the analytics surface visible and understandable before the user expands it.

Purpose: The analytics feature exists and works, but the collapsed-state trigger is too subtle to discover. This plan keeps the list-first, collapsed-by-default workflow while promoting analytics into a clearly visible summary CTA.
Output: A redesigned collapsed analytics trigger with accessible state, summary copy, and regression coverage.
</objective>

<execution_context>
@C:/Users/binsl/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/binsl/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-depth-differentiators/04-UAT.md
@.planning/phases/04-depth-differentiators/04-CONTEXT.md
@.planning/phases/04-depth-differentiators/04-03-SUMMARY.md
@.planning/debug/expense-analytics-toggle-affordance.md
</context>

<interfaces>
From [src/renderer/expenses/ExpenseAnalyticsSection.tsx](/D:/projects/Portfolio%20Projects/monolith/src/renderer/expenses/ExpenseAnalyticsSection.tsx):
```typescript
interface ExpenseAnalyticsSectionProps {
  analytics: ExpenseAnalytics | null
  isOpen: boolean
  trendMonths: 3 | 6 | 12
  onToggle: () => void
  onSelectTrendMonths: (months: 3 | 6 | 12) => void
}
```

From [src/renderer/expenses/ExpensesView.tsx](/D:/projects/Portfolio%20Projects/monolith/src/renderer/expenses/ExpensesView.tsx):
```typescript
const [showAnalytics, setShowAnalytics] = useState(false)

<ExpenseAnalyticsSection
  analytics={analytics}
  isOpen={showAnalytics}
  trendMonths={trendMonths}
  onToggle={() => setShowAnalytics((value) => !value)}
/>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lock the collapsed analytics discoverability contract in a component test</name>
  <files>tests/expense-analytics-section.test.tsx</files>
  <action>
    Create a renderer test that proves the collapsed analytics surface is discoverable without opening it:

    1. In the collapsed state, render a full-width interactive summary surface with `aria-expanded="false"`, a clear heading such as "Monthly analytics", and preview copy.
    2. When analytics data is available, the preview copy must include the current month label and total. When analytics is still null, the surface must still render visible placeholder copy instead of falling back to a near-invisible text link.
    3. Toggling the control must switch to `aria-expanded="true"` and reveal the existing chart section.
    4. Keep the feature collapsed by default; the test should assert discoverability, not auto-expansion.
  </action>
  <verify>
    <automated>rg -n "Monthly analytics|aria-expanded|monthLabel|monthTotal|placeholder" tests/expense-analytics-section.test.tsx</automated>
  </verify>
  <done>
    A dedicated component test file describes the required collapsed-state CTA, preview copy, and accessible expand/collapse behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the subtle text toggle with a visible analytics summary CTA</name>
  <files>
    src/renderer/expenses/ExpenseAnalyticsSection.tsx,
    src/renderer/expenses/ExpensesView.tsx,
    tests/expense-analytics-section.test.tsx
  </files>
  <behavior>
    - Test 1: The Expenses view still starts with analytics collapsed.
    - Test 2: The collapsed state shows a strong, visible summary CTA with month-total preview copy instead of a low-emphasis text button.
    - Test 3: Expanding preserves the existing monthly total header, donut chart, and 3/6/12 month trend controls above the list.
  </behavior>
  <action>
    Redesign the collapsed analytics affordance without changing the locked Phase 4 scope:

    1. Replace the small `Show charts` text button with a full-width bordered summary button or card. Use the existing token system, add `aria-expanded` and `aria-controls`, and include an icon/chevron only if it materially improves scanability.
    2. Show visible preview copy in the collapsed state, for example the active month label plus total and a short hint that category breakdown/trend charts are available. If analytics has not loaded yet, render a visible loading or placeholder summary instead of hiding the feature.
    3. Keep `showAnalytics` defaulting to `false` in `ExpensesView`. Do not turn analytics into a separate tab, route, or always-open section.
    4. Preserve the current expanded analytics content and dense list-first layout. The change here is affordance/discoverability, not a new analytics feature.
  </action>
  <verify>
    <automated>npx vitest run tests/expense-analytics-section.test.tsx</automated>
  </verify>
  <done>
    The analytics surface is obvious before expansion, still opens inline above the list, and remains collapsed by default while showing useful preview information.
  </done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/expense-analytics-section.test.tsx`.
Run `npm run build`.
</verification>

<success_criteria>
The expense analytics section is discoverable without guesswork: users can immediately see that monthly totals and charts exist, yet the module still preserves its collapsed-by-default, list-first workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-depth-differentiators/04-06-SUMMARY.md`
</output>
