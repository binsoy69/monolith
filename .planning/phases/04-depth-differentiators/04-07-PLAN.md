---
phase: 04-depth-differentiators
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/habits/HabitHeatmap.tsx
  - tests/habit-heatmap-layout.test.tsx
autonomous: true
gap_closure: true
requirements: [HAB-07]

must_haves:
  truths:
    - "Expanded habit heatmaps show readable month labels around tight boundaries such as Dec/Jan instead of overlapping or collapsing into the same slot."
    - "The 90-day heatmap still renders on the same weekly grid with the same accessible cell labels after the month-label fix."
  artifacts:
    - path: "src/renderer/habits/HabitHeatmap.tsx"
      provides: "Week-aligned month-label layout"
      contains: "buildMonthLabels"
    - path: "tests/habit-heatmap-layout.test.tsx"
      provides: "Heatmap month-label regression coverage"
      contains: "Dec"
  key_links:
    - from: "src/renderer/habits/HabitHeatmap.tsx"
      to: "tests/habit-heatmap-layout.test.tsx"
      via: "pure month-label helper and edge-case assertions"
      pattern: "buildMonthLabels"
---

<objective>
Close the heatmap month-label UAT gap.

Purpose: The heatmap cells and history summaries already work, but the month-label layout is unstable because labels are generated from day transitions rather than the rendered weekly grid. This plan fixes that specific HAB-07 regression without reopening the count-entry work.
Output: A week-aligned heatmap label builder with deterministic collision handling and focused regression coverage.
</objective>

<execution_context>
@C:/Users/binsl/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/binsl/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-depth-differentiators/04-UAT.md
@.planning/phases/04-depth-differentiators/04-CONTEXT.md
@.planning/phases/04-depth-differentiators/04-01-SUMMARY.md
@.planning/debug/habit-heatmap-month-labels.md
</context>

<interfaces>
From [src/renderer/habits/HabitHeatmap.tsx](/D:/projects/Portfolio%20Projects/monolith/src/renderer/habits/HabitHeatmap.tsx):
```typescript
interface HabitHeatmapProps {
  points: HabitHistoryPoint[]
}

const monthLabels = heatmapPoints.reduce<Array<{ label: string; column: number }>>(...)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define the heatmap month-label regression contract</name>
  <files>tests/habit-heatmap-layout.test.tsx</files>
  <action>
    Add a focused HAB-07 regression file for the month-label bug:

    1. Test the label-building helper rather than pixel-perfect screenshots.
    2. Cover adjacent month-boundary cases such as Dec/Jan where raw day transitions can fall into the same or neighboring week columns.
    3. Assert that the resulting label set is week-column aware and collision-safe: no duplicate labels in the same slot, and no unreadable same-column overlap scenario is emitted.
    4. Preserve the current 90-day weekly grid model; this is a layout fix, not a heatmap redesign.
  </action>
  <verify>
    <automated>rg -n "buildMonthLabels|Dec|Jan|column|collision" tests/habit-heatmap-layout.test.tsx</automated>
  </verify>
  <done>
    A dedicated heatmap regression file describes the month-boundary bug and the week-aligned collision rule that fixes it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement week-aligned month labels with deterministic collision handling</name>
  <files>
    src/renderer/habits/HabitHeatmap.tsx,
    tests/habit-heatmap-layout.test.tsx
  </files>
  <behavior>
    - Test 1: Month labels are derived from rendered week columns, not raw day transitions.
    - Test 2: Tight boundaries such as Dec/Jan do not produce overlapping or duplicate labels in the same slot.
    - Test 3: The existing 90-day heatmap grid and accessible cell labels remain unchanged aside from the month-label layout.
  </behavior>
  <action>
    Fix the heatmap label pipeline only:

    1. Extract or export a pure `buildMonthLabels` helper in `HabitHeatmap.tsx`.
    2. Generate labels from the same 13-week column model used to render the grid.
    3. Add a deterministic collision rule for same-column or adjacent-column boundaries, such as preferring the newer month or staggering rows. Pick one rule and encode it in tests.
    4. Do not change `HabitsView`, count-entry UI, lazy history loading, or cell accessibility behavior.
  </action>
  <verify>
    <automated>npx vitest run tests/habit-heatmap-layout.test.tsx</automated>
  </verify>
  <done>
    Heatmap month labels are stable and readable around boundaries such as Dec/Jan, while the rest of the 90-day heatmap behavior remains intact.
  </done>
</task>

</tasks>

<verification>
Run `npx vitest run tests/habit-heatmap-layout.test.tsx`.
Run `npm run build`.
</verification>

<success_criteria>
The Phase 04 heatmap-label gap is closed: expanded habit heatmaps present readable month labels even at tight month transitions, without regressing the existing 90-day grid or accessibility text.
</success_criteria>

<output>
After completion, create `.planning/phases/04-depth-differentiators/04-07-SUMMARY.md`
</output>
